// ОДНЕ ДЖЕРЕЛО ПРАВДИ
// Оголошення функцій-селекторів поза компонентами також добре тим, 
// що компоненти не знають про форму стану Redux, і у разі його зміни 
// достатньо буде відредагувати код одного файлу, 
// а не шукати селектори за кодом всіх компонентів програми.



export const getTasks = state => state.tasks;
export const getStatusFilter = state => state.filters.status;